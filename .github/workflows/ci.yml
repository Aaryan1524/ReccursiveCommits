name: CI

on:
  push:
  pull_request:

permissions:
  contents: read

env:
  CARGO_TERM_COLOR: always

jobs:
  quality:
    name: Format, lint, and build
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@stable
        with:
          components: clippy,rustfmt
      - run: cargo fmt --all -- --check
      - run: cargo clippy --workspace --all-targets --all-features -- -D warnings
      - run: cargo build --workspace --locked

  test:
    name: Test on ${{ matrix.os }}
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, macos-latest]
    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@stable
      - run: cargo test --workspace --all-targets --locked
      - run: cargo build --workspace --locked
      - run: ./tests/scenarios/phase1_cli_smoke.sh
      - run: ./tests/scenarios/phase2_capture_chain.sh
      - run: ./tests/scenarios/phase3_release_cli.sh
      - run: ./tests/scenarios/phase4_schedule_cli.sh
      - run: ./tests/scenarios/phase5_autonomous_release.sh
      - run: ./tests/scenarios/phase6_idempotent_handoff.sh
      - run: ./tests/scenarios/phase6_agent_submit.sh
      - run: ./tests/scenarios/phase6_capture_boundaries.sh
      - run: ./tests/scenarios/phase8_terminal_onboarding.sh
      - run: ./tests/scenarios/phase8_plan_authoring.sh
      - run: ./tests/scenarios/phase8_queue_and_watch.sh
      - run: ./tests/scenarios/phase8_unit_inspection.sh
      - run: ./tests/scenarios/phase8_diagnostics_and_controls.sh
      - run: ./tests/scenarios/phase8_terminal_journeys.sh
      - run: ./tests/scenarios/phase6_codex_integration.sh
      - run: ./tests/scenarios/phase6_agent_publication.sh
      - run: ./tests/scenarios/phase10_development_publication.sh
      - run: ./tests/scenarios/phase10_branch_protections.sh
      - run: ./tests/scenarios/phase10_pull_requests.sh
